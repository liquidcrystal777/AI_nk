import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.aink.app",
  appName: "AI-nk",
  webDir: "out",
  server: {
    androidScheme: "https",
  },
};

export default config;